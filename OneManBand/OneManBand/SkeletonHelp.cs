using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace OneManBand
{
    class SkeletonHelp
    {
        public SkeletonHelp()
        {

        }

        //public void drawJoint(Joint joint, Texture2D image)
        //{
        //    Joint scaledJoint = joint.Scale(screenWidth, screenHeight);
        //    Vector2 position = new Vector2(scaledJoint.Position.X, scaledJoint.Position.Y);
        //    spriteBatch.Draw(image, position, null, Color.White, 0f, Vector2.Zero, 0.2f / scaledJoint.Position.Z, SpriteEffects.None, 0);

        //    //Vector2 jPos = new Vector2(joint.Position.X, joint.Position.Y);
        //    //spriteBatch.Draw(image, jPos, null, Color.White, 0f, Vector2.Zero, 0.2f, SpriteEffects.None, 0); 
        //}
    }
}
